package patterns.FactoryDesignPattern.Factory;

public class PointFactory {

    /*

    Esta implementación no se utiliza por que el constructor de la clase Point es privada,
    y se utiliza en la misma clase Point se crea la clase statica para porder utilizarla
    ver clase Point.java.

    public static Point newCartesianPoint(double x, double y) {
        return new Point(x, y);
    }

    public static Point newPolarPoint(double rho, double theta) {
        return new Point(rho*Math.cos(theta), rho*Math.sin(theta));
    }
    */
}
